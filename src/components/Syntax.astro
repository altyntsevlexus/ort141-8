---
interface Props {
  code: string;
  label?: string;
}
const { code, label = 'Синтаксис' } = Astro.props;
---
<div class="box">
  <div class="box__label">{label}</div>
  <div class="box__code">{code}</div>
  {Astro.slots.has('default') && <div class="box__note"><slot /></div>}
</div>
